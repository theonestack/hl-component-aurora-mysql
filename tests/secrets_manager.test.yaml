test_metadata:
  type: config
  name: use secrets manager credentials
  description: use secrets manager for master username and password credentials

secret_username: payments
storage_encrypted: true